import { SimulatorShell } from "@/components/simulator/simulator-shell";

export const metadata = {
  title: "Simulator — Parkly",
};

export default function SimulatorPage() {
  return <SimulatorShell />;
}
